# github actions test

# Setup:

create python venv

```
python3 -m venv .env
```

use the venv

```
source .env/bin/activate
```

install the requirements

```
python3 -m pip3 -r requirements.txt
```
